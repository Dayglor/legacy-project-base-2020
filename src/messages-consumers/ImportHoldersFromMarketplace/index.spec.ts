describe('ImportHoldersFromMarketplace Tests', () => {
	it('should import all holders from a marketplace', async () => {
		// const job = container.resolve(ImportHoldersFromMarketplace);
		// const holderRepository = container.resolve(MysqlHoldersRepository);
		// const marketplaceId = '4d2c80162c98410c8df356b954416a26';
		// await job.execute({ marketplaceId, limit: 4, allPages: false });
		// const holders = await holderRepository.findByMarketplaceId(marketplaceId);
		// expect(holders.length).toEqual(5);
	});
});
